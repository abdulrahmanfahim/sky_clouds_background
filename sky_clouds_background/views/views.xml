<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <template id="sky_clouds_background_layout" name="Bright Daytime Cloudscape" inherit_id="website.layout" priority="50">
        <!-- Add styles for sky background -->
        <xpath expr="//head" position="inside">
            <style type="text/css">
                <![CDATA[
                /* ===== SKY BACKGROUND IMAGE ===== */
                .sky-background {
                    position: fixed;
                    top: 0;
                    left: 0;
                    width: 100%;
                    height: 100%;
                    background-image: url('/sky_clouds_background/static/description/background.jpg');
                    background-size: cover;
                    background-position: center;
                    background-repeat: no-repeat;
                    z-index: -3;
                    pointer-events: none;
                }

                /* ===== GLOSSY OVERLAY FOR 4TH WALL EFFECT ===== */
                .glossy-overlay {
                    position: fixed;
                    top: 0;
                    left: 0;
                    width: 100%;
                    height: 100%;
                    background: linear-gradient(125deg, 
                        rgba(255,255,255,0.25) 0%, 
                        rgba(255,255,255,0.1) 30%, 
                        rgba(255,255,255,0.15) 70%, 
                        transparent 100%);
                    pointer-events: none;
                    z-index: 10;
                    mix-blend-mode: overlay;
                    backdrop-filter: blur(1px);
                }

                /* ===== CLOUDS - SMALLER & NEAR TOP ===== */
                .cloud {
                    position: fixed;
                    background-image: url('/sky_clouds_background/static/description/cloud.png');
                    background-size: contain;
                    background-repeat: no-repeat;
                    pointer-events: none;
                    z-index: -2;
                    opacity: 0.9;
                    will-change: transform;
                }

                /* Reduced cloud sizes to avoid overlapping */
                .cloud-small {
                    width: 60px;
                    height: 30px;
                }
                .cloud-medium {
                    width: 80px;
                    height: 40px;
                }
                .cloud-large {
                    width: 100px;
                    height: 50px;
                }
                .cloud-xlarge {
                    width: 120px;
                    height: 60px;
                }

                /* ===== REALISTIC RAIN ANIMATION ===== */
                .rain-container {
                    position: fixed;
                    top: -10%;
                    left: 0;
                    width: 100%;
                    height: 120%;
                    pointer-events: none;
                    z-index: -1;
                    overflow: hidden;
                }

                .raindrop {
                    position: absolute;
                    width: 2px;
                    height: 25px;
                    background: linear-gradient(to bottom, 
                        rgba(200,230,255,0.9) 0%, 
                        rgba(160,200,230,0.8) 40%,
                        rgba(120,160,200,0.6) 100%);
                    border-radius: 0 0 4px 4px;
                    animation: rain linear infinite;
                    opacity: 0.5;
                    box-shadow: 0 0 6px rgba(255,255,255,0.4);
                }

                @keyframes rain {
                    0% {
                        transform: translateY(-10vh) translateX(0);
                        opacity: 0.7;
                    }
                    100% {
                        transform: translateY(110vh) translateX(15px);
                        opacity: 0.2;
                    }
                }

                /* Responsive adjustments */
                @media (max-width: 768px) {
                    .cloud-small { width: 40px; height: 20px; }
                    .cloud-medium { width: 55px; height: 28px; }
                    .cloud-large { width: 70px; height: 35px; }
                    .cloud-xlarge { width: 85px; height: 42px; }
                    
                    .raindrop {
                        height: 15px;
                        width: 1.5px;
                    }
                }

                /* Ensure content stays above background */
                #wrap {
                    position: relative;
                    z-index: 20;
                    background: transparent !important;
                }

                /* Navbar and interactive elements above everything */
                .navbar, header, o_main_navbar, o_menu_sections, o_menu_toggle {
                    position: relative;
                    z-index: 100 !important;
                }
                ]]>
            </style>
        </xpath>

        <!-- Add sky background and containers -->
        <xpath expr="//body" position="inside">
            <!-- Sky background image -->
            <div class="sky-background"></div>

            <!-- Cloud container (will be populated by JS) -->
            <div id="cloud-container"></div>

            <!-- Rain container (will be populated by JS) -->
            <div class="rain-container" id="rain-container"></div>

            <!-- Glossy overlay (4th wall effect) -->
            <div class="glossy-overlay"></div>
        </xpath>

        <!-- Add JavaScript for cloud and rain generation -->
        <xpath expr="//body" position="after">
            <script type="text/javascript">
                <![CDATA[
                document.addEventListener('DOMContentLoaded', function() {
                    const cloudContainer = document.getElementById('cloud-container');
                    const rainContainer = document.getElementById('rain-container');
                    const isMobile = window.innerWidth <= 768;
                    
                    // ===== CLOUD MANAGEMENT =====
                    const cloudSizes = ['small', 'medium', 'large', 'xlarge'];
                    const clouds = [];
                    const maxClouds = isMobile ? 4 : 6;   // Fewer clouds to avoid overlap
                    
                    // Create initial clouds
                    for (let i = 0; i < maxClouds; i++) {
                        createCloud(true);
                    }
                    
                    function createCloud(initial = false) {
                        const cloud = document.createElement('div');
                        
                        // Random size
                        const sizeIndex = Math.floor(Math.random() * cloudSizes.length);
                        cloud.className = `cloud cloud-${cloudSizes[sizeIndex]}`;
                        
                        // ===== Constrain clouds to top 50px =====
                        // Top is in pixels from the top edge, random between 0 and 50
                        const top = Math.random() * 50;  // 0 to 50px
                        cloud.style.top = top + 'px';
                        
                        // Random horizontal starting position
                        let left;
                        let direction;
                        
                        if (initial) {
                            // Initial clouds can be anywhere, including on-screen
                            if (Math.random() > 0.3) {
                                left = Math.random() * 100; // on-screen
                            } else {
                                left = -30 + Math.random() * 20; // off-screen left
                            }
                            direction = Math.random() > 0.5 ? 'right' : 'left';
                        } else {
                            // New clouds always start off-screen
                            if (Math.random() > 0.5) {
                                left = -40 + Math.random() * 20; // off-screen left
                                direction = 'right';
                            } else {
                                left = 120 + Math.random() * 20; // off-screen right
                                direction = 'left';
                            }
                        }
                        
                        cloud.style.left = left + '%';
                        
                        // Random speed (adjusted for smaller size)
                        let speed;
                        switch(sizeIndex) {
                            case 0: speed = 0.03 + Math.random() * 0.02; break; // small
                            case 1: speed = 0.02 + Math.random() * 0.016; break; // medium
                            case 2: speed = 0.014 + Math.random() * 0.012; break; // large
                            case 3: speed = 0.01 + Math.random() * 0.008; break; // xlarge
                        }
                        
                        // Random opacity
                        cloud.style.opacity = 0.6 + Math.random() * 0.3;
                        
                        cloudContainer.appendChild(cloud);
                        
                        // Store cloud data
                        clouds.push({
                            element: cloud,
                            left: left,
                            direction: direction || (Math.random() > 0.5 ? 'right' : 'left'),
                            speed: speed,
                            sizeIndex: sizeIndex
                        });
                    }
                    
                    // ===== RAIN GENERATION =====
                    if (rainContainer) {
                        rainContainer.innerHTML = '';
                        const raindropCount = isMobile ? 150 : 300;
                        
                        for (let i = 0; i < raindropCount; i++) {
                            const raindrop = document.createElement('div');
                            raindrop.className = 'raindrop';
                            
                            // Random position
                            raindrop.style.left = Math.random() * 100 + '%';
                            raindrop.style.top = (Math.random() * 100) + '%';
                            
                            // Random animation duration (realistic speed)
                            const duration = isMobile ? 1.5 + Math.random() * 1.5 : 2 + Math.random() * 2;
                            raindrop.style.animation = `rain ${duration}s linear infinite`;
                            
                            // Random delay
                            raindrop.style.animationDelay = (Math.random() * 3) + 's';
                            
                            // Random size variation
                            const height = isMobile ? 12 + Math.random() * 8 : 18 + Math.random() * 12;
                            raindrop.style.height = height + 'px';
                            
                            rainContainer.appendChild(raindrop);
                        }
                    }
                    
                    // ===== ANIMATION LOOP =====
                    function animateClouds() {
                        for (let i = clouds.length - 1; i >= 0; i--) {
                            const cloud = clouds[i];
                            
                            // Move cloud based on direction
                            if (cloud.direction === 'right') {
                                cloud.left += cloud.speed;
                            } else {
                                cloud.left -= cloud.speed;
                            }
                            
                            // Apply position
                            cloud.element.style.left = cloud.left + '%';
                            
                            // Check if cloud is completely off-screen
                            if (cloud.left > 140 || cloud.left < -40) {
                                // Remove cloud element
                                cloud.element.remove();
                                // Remove from array
                                clouds.splice(i, 1);
                                // Create new cloud to maintain count
                                createCloud(false);
                            }
                        }
                        
                        requestAnimationFrame(animateClouds);
                    }
                    
                    // Start animation
                    animateClouds();
                    
                    // Handle window resize
                    window.addEventListener('resize', function() {
                        // Could adjust cloud count based on new screen size
                        // For simplicity, we'll just maintain current behavior
                    });
                });
                ]]>
            </script>
        </xpath>
    </template>
</odoo>
